//Declara dos variables de tipo string. Imprime por consola el resultado de concatenarlas.

/*const prefix = 'par';
const subfix = 'aguas';

console.log (prefix + subfix);*/

const prefix = 'par';
const subfix = 'aguas';

console.log ('Hoy me llevaré el' + ' ' + prefix + subfix)
