//Crea una función que imprima por consola el string 'Hello World'.

const greetings = () => {
    return 'Hello World'
};
console.log (greetings());

//Otra manera mal hecha..

/*const greetings = () => {
    console.log ('Hello World');
};
greetings();

a la funcion siempre a que llamarla para que ejecute, y si la llamas sin console.log solo la ejecuta pero no la pinta. 
Para eso hay que llamar a la funcion en un console.log fuera de la funcición.
Igualmente no hay que retornar la funcion pero si no lo haces la funcion no te devuelve ningun valor, normalmente hará falta.*/
