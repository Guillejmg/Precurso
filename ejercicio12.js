//Crea una función que reciba un número como parámetro e imprima por consola si el número dado es par o impar.
//Nota: buscar como se clona un repo en github y revisar la la funcion de configuracion en terminal de git "main",¿que es. DIR?
const kindOfNumber = (X) => {
    if (x % 2 != 0) {
        console.log ('este numero es impar')
    } 
}