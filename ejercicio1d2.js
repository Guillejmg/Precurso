//Crea una función que elimine el primer y último caracter de un string recibido por parámetros.
const Wordstring = (a) => {
    Wordstring.toUpperCase (a)
    return (Wordstring.length - 1 && -5)
}

console.log (Wordstring (a = 'string'))